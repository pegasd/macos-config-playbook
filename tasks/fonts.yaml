---
#
# homebrew/cask-fonts was archived in 2024 -- font casks now live in
# homebrew/cask, which ships with Homebrew. No tap required.
#
- name: Fonts
  homebrew_cask:
    name: '{{ item }}'
    state: present
  loop: '{{ fonts }}'
...
